class AppConfig {
  // App Information
  static const String appTitle = "Shopping Collection";
  static const String appVersion = "1.0.0";
  
  // Messages
  static const String cartEmptyMessage = "Your cart is empty!";
  static const String selectOptionMessage = "Please Select an Option";
  static const String productAddedMessage = "Product added to Cart";
  static const String productRemovedMessage = "Product removed from cart";
  static const String cartClearedMessage = "Cart cleared";
  
  // Navigation
  static const List<String> categories = [
    "All",
    "Shoes",
    "Jeans",
    "Tshirts",
    "Glasses",
    "Watches",
    "Laptops",
    "Phones"
  ];

  // Dimensions
  static const double defaultPadding = 16.0;
  static const double borderRadius = 20.0;
  static const double cardElevation = 10.0;
  
  // Animation Durations
  static const Duration snackBarDuration = Duration(seconds: 2);
  static const Duration transitionDuration = Duration(milliseconds: 300);
}
